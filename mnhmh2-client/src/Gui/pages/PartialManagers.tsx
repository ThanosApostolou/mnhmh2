import React, { ReactNode } from "react";

export class PartialManagers extends React.Component {
    render(): ReactNode {
        return (
            <p>This is PartialManagers page</p>
        );
    }
}