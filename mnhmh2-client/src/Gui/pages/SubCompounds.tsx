import React, { ReactNode } from "react";

export class SubCompounds extends React.Component {
    render(): ReactNode {
        return (
            <p>This is SubCompounds page</p>
        );
    }
}